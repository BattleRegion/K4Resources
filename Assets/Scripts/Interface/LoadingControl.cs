using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class LoadingControl : MonoBehaviour
{

    //void OnEnable()
    //{
    //    System.Random r = new System.Random();
    //    int rNum = r.Next(0, MonsterGather.Count);
    //    GameObject g = NGUITools.AddChild(gameObject,MonsterGather[rNum]);
    //    g.transform.localScale = new Vector3(0.1f, 0.1f, 0.1f);
    //    g.transform.localPosition = new Vector3(0f, 0f, -0.05f);
    //    g.GetComponent<Animator>().SetInteger("ActionState", 40);
    //    PetUIController.SetLayer(g.transform, LayerHelper.Cover);
    //}
}
